[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E501",  # line-too-long — analysis code uses intentional long annotation strings
    "W291",  # trailing-whitespace
    "W293",  # whitespace-before-comment
    "E221",  # multiple-spaces-before-operator — visual alignment
    "E241",  # multiple-spaces-after-comma
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
